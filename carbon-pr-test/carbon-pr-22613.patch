diff --git a/packages/styles/scss/components/__tests__/modal-test.js b/packages/styles/scss/components/__tests__/modal-test.js
index 0f57258eef81..dfe922b1cbf9 100644
--- a/packages/styles/scss/components/__tests__/modal-test.js
+++ b/packages/styles/scss/components/__tests__/modal-test.js
@@ -1,5 +1,5 @@
 /**
- * Copyright IBM Corp. 2018, 2023
+ * Copyright IBM Corp. 2018, 2026
  *
  * This source code is licensed under the Apache-2.0 license found in the
  * LICENSE file in the root directory of this source tree.
@@ -9,6 +9,7 @@
 
 'use strict';
 
+const postcss = require('postcss');
 const { SassRenderer } = require('@carbon/test-utils/scss');
 
 const { render } = SassRenderer.create(__dirname);
@@ -24,4 +25,38 @@ describe('scss/components/modal', () => {
      `);
     expect(unwrap('mixin')).toBe(true);
   });
+
+  test('hover styles are only applied on devices that support hover', async () => {
+    const { result } = await render(`
+      @use '../modal';
+    `);
+    const hoverRules = [];
+
+    postcss.parse(result.css.toString()).walkRules((rule) => {
+      if (rule.selector.includes('.cds--modal-close:hover')) {
+        hoverRules.push(rule);
+      }
+    });
+
+    expect(hoverRules.length).toBeGreaterThan(0);
+    expect(
+      hoverRules.every((rule) => {
+        let parent = rule.parent;
+
+        while (parent) {
+          if (
+            parent.type === 'atrule' &&
+            parent.name === 'media' &&
+            parent.params.includes('(any-hover: hover)')
+          ) {
+            return true;
+          }
+
+          parent = parent.parent;
+        }
+
+        return false;
+      })
+    ).toBe(true);
+  });
 });
diff --git a/packages/styles/scss/components/modal/_modal.scss b/packages/styles/scss/components/modal/_modal.scss
index 8e04be62149c..a722c04de6a9 100644
--- a/packages/styles/scss/components/modal/_modal.scss
+++ b/packages/styles/scss/components/modal/_modal.scss
@@ -363,8 +363,10 @@
     inline-size: 3rem;
     transition: background-color $duration-fast-02 motion(standard, productive);
 
-    &:hover {
-      background-color: $layer-hover;
+    @media (any-hover: hover) {
+      &:hover {
+        background-color: $layer-hover;
+      }
     }
 
     &:focus {
