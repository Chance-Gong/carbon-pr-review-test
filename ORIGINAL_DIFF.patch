diff --git a/packages/carbon-components-react/tsdown.config.mts b/packages/carbon-components-react/tsdown.config.mts
--- a/packages/carbon-components-react/tsdown.config.mts
+++ b/packages/carbon-components-react/tsdown.config.mts
@@ -11,10 +11,12 @@ const BANNER = `/**
 const sharedConfig = defineConfig({
   banner: BANNER,
   clean: false,
+  deps: {
+    neverBundle: ['@carbon/react'],
+  },
   dts: false,
   entry: ['src/index.js'],
-  external: ['@carbon/react'],
-  failOnWarn: false,
+  failOnWarn: true,
   logLevel: 'warn',
   outputOptions(options) {
     return {

diff --git a/packages/cli/src/commands/bundle/tsdown.js b/packages/cli/src/commands/bundle/tsdown.js
--- a/packages/cli/src/commands/bundle/tsdown.js
+++ b/packages/cli/src/commands/bundle/tsdown.js
@@ -48,12 +48,14 @@ export default async function bundle(entrypoint, options) {
   for (const { format, directory } of outputFolders) {
     await build({
       clean: false,
+      deps: {
+        neverBundle: Object.keys(dependencies),
+      },
       // Emit declarations in a separate pass so we can mirror the same `.d.ts`
       // tree to `es`, `lib`, and `umd`.
       dts: false,
       entry: [entrypoint],
-      external: Object.keys(dependencies),
-      failOnWarn: false,
+      failOnWarn: true,
       format,
       globalName: format === 'umd' ? name : undefined,
       outDir: directory,

diff --git a/packages/feature-flags/tsdown.config.mts b/packages/feature-flags/tsdown.config.mts
--- a/packages/feature-flags/tsdown.config.mts
+++ b/packages/feature-flags/tsdown.config.mts
@@ -13,7 +13,7 @@ const sharedConfig = defineConfig({
   clean: false,
   dts: false,
   entry: ['src/index.js'],
-  failOnWarn: false,
+  failOnWarn: true,
   logLevel: 'warn',
   outputOptions(options) {
     return {

diff --git a/packages/icon-build-helpers/src/builders/react/builder.js b/packages/icon-build-helpers/src/builders/react/builder.js
--- a/packages/icon-build-helpers/src/builders/react/builder.js
+++ b/packages/icon-build-helpers/src/builders/react/builder.js
@@ -20,7 +20,12 @@ const BANNER = `/**
  *
  * Code generated by @carbon/icon-build-helpers. DO NOT EDIT.
  */`;
-const external = ['@carbon/icon-helpers', 'react', 'prop-types'];
+const external = [
+  '@carbon/icon-helpers',
+  'react',
+  'react/jsx-runtime',
+  'prop-types',
+];
 async function builder(metadata, { output }) {
   const { build: tsdown } = await import('tsdown');
   // Using the metadata, we can build up a list of modules that we want included
@@ -126,10 +131,12 @@ const didWarnAboutDeprecation = {};`;
   for (const { directory, format } of bundles) {
     await tsdown({
       clean: false,
+      deps: {
+        neverBundle: external,
+      },
       dts: false,
       entry: input,
-      external,
-      failOnWarn: false,
+      failOnWarn: true,
       format: format === 'commonjs' ? 'cjs' : 'esm',
       logLevel: 'warn',
       inputOptions(inputOptions) {
@@ -158,12 +165,14 @@ const didWarnAboutDeprecation = {};`;
 
   await tsdown({
     clean: false,
+    deps: {
+      neverBundle: external,
+    },
     dts: false,
     entry: {
       index: 'index.ts',
     },
-    external,
-    failOnWarn: false,
+    failOnWarn: true,
     format: 'iife',
     globalName: 'CarbonIconsReact',
     logLevel: 'warn',
@@ -187,6 +196,7 @@ const didWarnAboutDeprecation = {};`;
           '@carbon/icon-helpers': 'CarbonIconHelpers',
           'prop-types': 'PropTypes',
           react: 'React',
+          'react/jsx-runtime': 'ReactJSXRuntime',
         },
       };
     },

diff --git a/packages/icon-build-helpers/src/builders/react/next.js b/packages/icon-build-helpers/src/builders/react/next.js
--- a/packages/icon-build-helpers/src/builders/react/next.js
+++ b/packages/icon-build-helpers/src/builders/react/next.js
@@ -180,10 +180,12 @@ async function builder(metadata, { output }) {
   for (const target of targets) {
     await tsdown({
       clean: false,
+      deps: {
+        neverBundle: external,
+      },
       dts: false,
       entry: input,
-      external,
-      failOnWarn: false,
+      failOnWarn: true,
       format: target.format === 'commonjs' ? 'cjs' : 'esm',
       logLevel: 'warn',
       inputOptions(inputOptions) {

diff --git a/packages/icon-build-helpers/src/builders/vanilla.js b/packages/icon-build-helpers/src/builders/vanilla.js
--- a/packages/icon-build-helpers/src/builders/vanilla.js
+++ b/packages/icon-build-helpers/src/builders/vanilla.js
@@ -60,7 +60,7 @@ async function builder(metadata, { output }) {
       clean: false,
       dts: false,
       entry: input,
-      failOnWarn: false,
+      failOnWarn: true,
       format: format === 'commonjs' ? 'cjs' : 'esm',
       logLevel: 'warn',
       inputOptions(inputOptions) {
@@ -88,7 +88,7 @@ async function builder(metadata, { output }) {
     entry: {
       index: 'index.js',
     },
-    failOnWarn: false,
+    failOnWarn: true,
     format: 'iife',
     globalName: 'CarbonIcons',
     logLevel: 'warn',

diff --git a/packages/icon-build-helpers/src/builders/vue/builder.js b/packages/icon-build-helpers/src/builders/vue/builder.js
--- a/packages/icon-build-helpers/src/builders/vue/builder.js
+++ b/packages/icon-build-helpers/src/builders/vue/builder.js
@@ -74,10 +74,12 @@ async function builder(metadata, { output }) {
   for (const { directory, format } of bundles) {
     await tsdown({
       clean: false,
+      deps: {
+        neverBundle: external,
+      },
       dts: false,
       entry: input,
-      external,
-      failOnWarn: false,
+      failOnWarn: true,
       format: format === 'commonjs' ? 'cjs' : 'esm',
       logLevel: 'warn',
       inputOptions(inputOptions) {
@@ -101,12 +103,14 @@ async function builder(metadata, { output }) {
 
   await tsdown({
     clean: false,
+    deps: {
+      neverBundle: external,
+    },
     dts: false,
     entry: {
       index: 'index.js',
     },
-    external,
-    failOnWarn: false,
+    failOnWarn: true,
     format: 'iife',
     globalName: 'CarbonIconsVue',
     logLevel: 'warn',

diff --git a/packages/react/tasks/build.js b/packages/react/tasks/build.js
--- a/packages/react/tasks/build.js
+++ b/packages/react/tasks/build.js
@@ -83,6 +83,10 @@ async function build() {
     await tsdown({
       banner,
       clean: false,
+      deps: {
+        neverBundle: external,
+        onlyBundle: false,
+      },
       // `tsdown`/`rolldown-plugin-dts` bundles declarations. `@carbon/react`
       // intentionally publishes a wide `es/` + `lib/` file tree, and consumers
       // deep-import those paths. If we let tsdown own declaration generation we
@@ -91,9 +95,7 @@ async function build() {
       // type file.
       dts: false,
       entry: [reactEntrypoint, ...reactCompatEntrypoints],
-      external,
-      inlineOnly: false,
-      failOnWarn: false,
+      failOnWarn: true,
       format: format.type,
       logLevel: 'warn',
       loader: {
@@ -109,6 +111,7 @@ async function build() {
           ...options,
           chunkFileNames: '[name].js',
           entryFileNames: '[name].js',
+          exports: format.type === 'cjs' ? 'named' : undefined,
         };
       },
       platform: 'browser',
@@ -139,11 +142,13 @@ async function build() {
   await tsdown({
     banner,
     clean: false,
+    deps: {
+      neverBundle: external,
+      onlyBundle: false,
+    },
     dts: true,
     entry: [iconsEntrypoint],
-    external,
-    inlineOnly: false,
-    failOnWarn: false,
+    failOnWarn: true,
     format: 'cjs',
     logLevel: 'warn',
     loader: {
@@ -167,11 +172,13 @@ async function build() {
   await tsdown({
     banner,
     clean: false,
+    deps: {
+      neverBundle: external,
+      onlyBundle: false,
+    },
     dts: false,
     entry: [iconsEntrypoint],
-    external,
-    inlineOnly: false,
-    failOnWarn: false,
+    failOnWarn: true,
     format: 'esm',
     logLevel: 'warn',
     loader: {
@@ -265,11 +272,7 @@ async function emitReactDeclarations(tsconfigPath, outDir) {
   }
 
   if (diagnostics.length > 0) {
-    // Some source files currently report type issues during declaration emit,
-    // but TypeScript can still write the declaration files we need. Surface the
-    // diagnostics so they are visible during builds without making this package
-    // fail to publish.
-    console.warn(formatDiagnostics(diagnostics));
+    throw new Error(formatDiagnostics(diagnostics));
   }
 }
 

diff --git a/packages/upgrade/tsdown.config.mts b/packages/upgrade/tsdown.config.mts
--- a/packages/upgrade/tsdown.config.mts
+++ b/packages/upgrade/tsdown.config.mts
@@ -2,13 +2,16 @@ import { defineConfig } from 'tsdown';
 
 export default defineConfig({
   clean: false,
+  deps: {
+    neverBundle: ['jscodeshift'],
+    onlyBundle: false,
+  },
   dts: false,
   entry: ['src/cli.js'],
-  inlineOnly: false,
+  failOnWarn: true,
   logLevel: 'warn',
   platform: 'node',
   target: 'node14',
-  external: ['jscodeshift'],
   outputOptions(options) {
     return {
       ...options,

diff --git a/packages/utilities-react/tsdown.config.mts b/packages/utilities-react/tsdown.config.mts
--- a/packages/utilities-react/tsdown.config.mts
+++ b/packages/utilities-react/tsdown.config.mts
@@ -8,6 +8,7 @@ const sharedConfig = defineConfig({
     index: 'src/index.ts',
     '*': ['src/*/index.{ts,tsx,js,jsx}'],
   },
+  failOnWarn: true,
   logLevel: 'warn',
   target: 'es2022',
   outputOptions(options) {

diff --git a/packages/utilities/tsdown.config.mts b/packages/utilities/tsdown.config.mts
--- a/packages/utilities/tsdown.config.mts
+++ b/packages/utilities/tsdown.config.mts
@@ -6,6 +6,7 @@ const sharedConfig = defineConfig({
     index: 'src/index.ts',
     '*': ['src/*/index.ts'],
   },
+  failOnWarn: true,
   logLevel: 'warn',
   target: 'es2022',
   outputOptions(options) {

diff --git a/packages/web-components/src/components/button/button-skeleton.ts b/packages/web-components/src/components/button/button-skeleton.ts
--- a/packages/web-components/src/components/button/button-skeleton.ts
+++ b/packages/web-components/src/components/button/button-skeleton.ts
@@ -46,8 +46,8 @@ class CDSButtonSkeleton extends CDSButton {
     const classes = classMap({
       [`${prefix}--btn`]: true,
       [`${prefix}--skeleton`]: true,
-      [`${prefix}--btn--${size}`]: size,
-      [`${prefix}--layout--size-${size}`]: size,
+      [`${prefix}--btn--${size}`]: size !== undefined,
+      [`${prefix}--layout--size-${size}`]: size !== undefined,
     });
     return href
       ? html`

diff --git a/packages/web-components/tasks/build-dist.js b/packages/web-components/tasks/build-dist.js
--- a/packages/web-components/tasks/build-dist.js
+++ b/packages/web-components/tasks/build-dist.js
@@ -58,18 +58,20 @@ async function buildDist() {
 
   await tsdown({
     clean: false,
+    deps: {
+      alwaysBundle: [/.*/],
+      neverBundle: [],
+      onlyBundle: false,
+    },
     dts: false,
     entry,
-    external: [],
-    failOnWarn: false,
+    failOnWarn: true,
     format: 'esm',
     inputOptions: withInputCompatibilityAndPlugins({
       postCSSPlugins,
       packageRoot,
     }),
-    inlineOnly: false,
     logLevel: 'warn',
-    noExternal: [/.*/],
     minify: true,
     outDir: path.resolve(packageRoot, 'dist'),
     outputOptions(options) {

diff --git a/packages/web-components/tasks/build.js b/packages/web-components/tasks/build.js
--- a/packages/web-components/tasks/build.js
+++ b/packages/web-components/tasks/build.js
@@ -69,14 +69,16 @@ async function build() {
     await tsdown({
       banner,
       clean: false,
+      deps: {
+        neverBundle: external,
+      },
       // use tsc for declaration emit instead of tsdown's dts plugin.
       // tsdown/rolldown-plugin-dts does not correctly handle mixin patterns
       // (e.g. HostListenerMixin) — it strips the generic base class type,
       // breaking downstream consumers
       dts: false,
       entry: format.inputs.map((input) => path.resolve(packageRoot, input)),
-      external,
-      failOnWarn: false,
+      failOnWarn: true,
       format: format.type,
       inputOptions: withInputCompatibilityAndPlugins,
       logLevel: 'warn',
@@ -254,7 +256,7 @@ async function emitDeclarations(tsconfigPath, outDir) {
   }
 
   if (diagnostics.length > 0) {
-    console.warn(formatDiagnostics(diagnostics));
+    throw new Error(formatDiagnostics(diagnostics));
   }
 }
 